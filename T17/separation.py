# Reading the input string from the user
input_sentence = input("Type in a sentence : ")

# Splitting the sentence into words in a list
sentence_list = input_sentence.split()

# Printing each element in the list
for word in sentence_list:
    print(word)